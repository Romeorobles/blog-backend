// area_triangulo.ts

function calcularAreaTriangulo(base: number, altura: number): number {
    return (base * altura) / 2;
}

console.log(calcularAreaTriangulo(10, 5));   // 25
console.log(calcularAreaTriangulo(8, 3));    // 12
console.log(calcularAreaTriangulo(6, 4));    // 12
